!!!
%html
  %head
    = partial :head
    %script{ :src => "/script/lib/zepto.min.js", :type => "text/javascript" }
    %script{ :src => "/script/lib/pdf.js/build/pdf.js", :type => "text/javascript" }
    %script{ :src => "/script/drag-drop.js", :type => "text/javascript" }
    %script{ :src => "/script/pdf-mode.js", :type => "text/javascript" }
  %body#categorize
    %h1 Categorize the Articles
    %hr
    #sort
      #sidebar
        .category
          .counter 1
          %h2 For My Side
          %span.description 
            = side_a
          %div.sortable-set{:ondragover => "dragover_handler(event)", :ondrop => "drop_handler(event)"}
            - articles.each do |article|
              %img{:src => "style/images/article.png"}
        %hr
        .category
          .counter 
          %h2 Against My Side
          %span.description 
            = side_b
            %div.sortable-set{:ondragover => "dragover_handler(event)", :ondrop => "drop_handler(event)"}
              - articles.each do |article|
                %img{:src => "style/images/article.png"}

      #content
        %h2 Uncategorized
        %p Please classify each article as either supporting your side or opposing your side.
        %div.sortable-set{:ondragover => "dragover_handler(event)", :ondrop => "drop_handler(event)"}
          - articles.each_with_index do |article, i|
            %div.sortable{:draggable => "true", :ondragstart => "dragstart_handler(event)"}
              %div.text
                %span.big 
                  = article['title']
                %span.small
                  = article['description']
              %a{:href=>"/#{uid}/article/#{i}"}
                Read Article 
